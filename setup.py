from setuptools import setup, find_packages

setup(
    name='dfutils',
    version='1.0',
    description='Dataframe utilities',
    author='B.K-SEO',
    packages=find_packages(),
    install_requires=[
        # list any dependencies your module requires
    ],
)
